#ifndef FORM_H
#define FORM_H

#include <string>
#include <vector>

namespace gawron
{

using FormName = std::string;
using FormValue = std::string;
using FormList = std::vector<FormValue>;

}

#endif
